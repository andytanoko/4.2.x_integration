# whether to enforce the strict check on the process def during update
#     true: It will check whether the process def corresponding process instance state is still running. if so, no update is allowed.
#     no: no checking will be performed, it will update the process def as normal
# After the change, restart is not needed
is.enable.strict.check=true